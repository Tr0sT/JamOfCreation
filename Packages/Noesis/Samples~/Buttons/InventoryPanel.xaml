<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <UserControl.Resources>
    <DataTemplate x:Key="InventoryItemTemplate">
      <Border Padding="8" CornerRadius="6" Background="#232428" Margin="0,6,0,6">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
          <Rectangle Width="36" Height="36" Fill="#2B2B2B" RadiusX="6" RadiusY="6" />
          <StackPanel Margin="10,0,0,0">
            <TextBlock Text="{Binding Name}" Foreground="#FFF" FontSize="16"/>
            <TextBlock Text="{Binding Description}" Foreground="#BEBEBE" FontSize="12"/>
          </StackPanel>
        </StackPanel>
      </Border>
    </DataTemplate>
  </UserControl.Resources>

  <StackPanel Margin="18,0,0,0">
    <TextBlock Text="ИНВЕНТАРЬ:" FontSize="22" Foreground="#FFF" Margin="0,0,0,8"/>
    <ScrollViewer Height="360" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding InventoryPanelViewModel.Items}" ItemTemplate="{StaticResource InventoryItemTemplate}"/>
    </ScrollViewer>
    <Button Content="МАГАЗИН" Height="48" Margin="0,12,0,0" HorizontalAlignment="Stretch" />
  </StackPanel>
</UserControl>